@model X.PagedList.IPagedList<DPLK.Models.CoiTypeRate>
@using X.PagedList.Mvc.Core

<link href="~/css/PagedList.css" rel="stylesheet" type="text/css" />
@{
    ViewData["Title"] = "DPLK | Perta Life";
    Layout = "~/Views/Shared/DPLK_Layout.cshtml";
}

<div class="container-xxl flex-grow-1 container-p-y">
    <div class="table-responsive text-nowrap">
        <div class="card-header">
            <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Pension /</span> Parameter Maker - COI Status Type</h4>
            @if (!string.IsNullOrEmpty(TempData["SuccessMessage"] as string))
            {
                <div class="alert alert-success" role="alert">
                    @TempData["SuccessMessage"]
                </div>
            }
            <a class="btn btn-primary text-end" asp-action="CreateVaGroup"><span class="tf-icons bx bx-book-add"></span>&nbsp; Create New</a>

            <div class="row mt-3">
                <div class="col-md-6">
                    <form asp-action="ParameterMakerSuspenseStatusType" method="get">
                        <div class="input-group mb-3">
                            <input type="text" name="searchString" class="form-control" placeholder="Search...">
                            <button class="btn btn-outline-secondary" type="submit">Search</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>
                            <a asp-action="ParameterMakerCOIStatusRate" asp-route-sortOrder="CoiTypeNmbr" asp-route-page="1">ID</a>
                        </th>
                        <th>
                            <a asp-action="ParameterMakerCOIStatusRate" asp-route-sortOrder="CoiAge" asp-route-page="1">Age</a>
                        </th>

                        <th>
                            <a asp-action="ParameterMakerCOIStatusRate" asp-route-sortOrder="CoiRate" asp-route-page="1">Coi Rate</a>
                        </th>
                        <th>
                            <a asp-action="ParameterMakerCOIStatusRate" asp-route-sortOrder="CoiAmount" asp-route-page="1">Coi Amount</a>
                        </th>
                        <th>
                            <a asp-action="ParameterMakerCOIStatusRate" asp-route-sortOrder="LastChangeDt" asp-route-page="1">Last Change Date</a>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var invType in Model)
                    {
                        <tr>
                            <td>@invType.CoiTypeNmbr</td>
                            <td>@invType.CoiAge</td>
                            <td>@invType.CoiRate</td>
                            <td>@invType.CoiAmount</td>
                            <td>@invType.LastChangeDt</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div class="text-center mt-4">
        @Html.PagedListPager(Model, page => Url.Action("ParameterMakerSuspenseStatusType", new { page, currentFilter = ViewBag.CurrentFilter, sortOrder = ViewBag.CurrentSort }))
    </div>
</div>
