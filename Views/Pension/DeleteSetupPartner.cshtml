@model DPLK.Models.DplkRollover

@{
    ViewData["Title"] = "DPLK | Perta Life";
    Layout = "~/Views/Shared/DPLK_Layout.cshtml";
}

<div class="container-xxl flex-grow-1 container-p-y">
    <div class="card my-4">
        <div class="card-body">
            <h2 class="fw-bold">Delete Confirmation</h2>
            <p class="lead">Are you sure you want to delete this partner?</p>

            <form method="post" action="@Url.Action("DeleteSetupPartnerAction", "Pension", new { id = ViewBag.DplkNmbr })" class="d-inline">
                <button type="submit" class="btn btn-danger">Yes, Delete</button>
            </form>

            <a href="@Url.Action("SetupPartner", "Pension")" class="btn btn-secondary">Cancel</a>
        </div>
    </div>
</div>
