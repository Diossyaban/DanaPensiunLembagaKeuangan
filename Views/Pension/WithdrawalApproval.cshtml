@{
    ViewData["Title"] = "DPLK | Perta Life";
    Layout = "~/Views/Shared/DPLK_Layout.cshtml";
}


<h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light"> Pension / </span> Transaction - Withdrawal Requested Fund Approval</h4>
@using (Html.BeginForm("Index", "Pension", FormMethod.Get))
{

    <div class="card-mb-4">
        <div class="card-header">
            <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">  </span> Table</h4>

            <div class="table-responsive text-nowrap">
                <table class="table">
                    <div class="row">
                        @*      <div class="col-sm-10">
                    <br />
                    <div class="row">
                    <div class="col-lg-12">
                    <div class="row">
                    <div class="col-lg-6 col-md-6 col-sm-12">
                    </div>
                    <div class="input-group mb-1">
                    @Html.TextBox("SearchString",ViewBag.CurrentFilter as string, new{@InputType="text", placeholder="Search...", @class="form-control", @id="search", @name="search"})
                    <button type="submit" class="btn btn-info" id="button-addon2">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                    <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"></path>
                    </svg>
                    </button>
                    </div>
                    </div>
                    </div>
                    </div>
                    </div>
                    <br />*@
                        @*<div class="col-sm-2">
                    Sort By : @Html.DropDownList("PageSize",
                    ViewBag.PageSize as SelectList,
                    new{
                    @class="form-control search-slt",
                    @placeholder="Page Size",
                    @onchange="getPagedSize(this.value)"
                    })
                    </div>*@
                    </div>
            </div>
        </div>
    </div>
}
<thead>
    <tr>
        <th>
            @*            @Html.DisplayNameFor(model => model.FirstOrDefault().DepartmentId)
            *@
        </th>
        <th>
            @*            @Html.DisplayNameFor(model => model.FirstOrDefault().Name)
            *@
        </th>
        <th>
            @*            @Html.DisplayNameFor(model => model.FirstOrDefault().GroupName)
            *@
        </th>
        <th>
            <a asp-action="Index" asp-route-sortOrder="@ViewBag.DepartmentId">
            </a>
        </th>
        <th></th>
    </tr>
</thead>
<tbody>
    @*    @foreach (var item in Model)
    *@
    <tr>
        <td>
            @*                @Html.DisplayFor(modelItem => item)
            *@
        </td>
        <td>
            @*                @Html.DisplayFor(modelItem => item)
            *@
        </td>
        <td>
            @*                @Html.DisplayFor(modelItem => item)
            *@
        </td>
        <td>
            <div class="dropdown">
                <button type="button"
                        class="btn p-0 dropdown-toggle hide-arrow"
                        data-bs-toggle="dropdown">
                    <i class="bx bx-dots-vertical-rounded"></i>
                </button>
                <div class="dropdown-menu">
                    <a class="dropdown-item" asp-action="Edit" asp-route-id=""><i class="bx bx-edit-alt me-1"></i>Edit</a>
                    <a class="dropdown-item" asp-action="Details" asp-route-id=""><i class="bx bx-detail me-1"></i>Details</a>
                    <a class="dropdown-item" asp-action="Delete" asp-route-id=""><i class="bx bx-trash me-1"></i>Delete</a>
                </div>
            </div>
        </td>
    </tr>


</tbody> </table>