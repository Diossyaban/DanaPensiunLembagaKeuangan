@model DPLK.Models.VClaimRequest2
@{
    ViewData["Title"] = "DPLK | Perta Life";
    Layout = "~/Views/Shared/DPLK_Layout.cshtml";
}


<h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light"> Pension / </span> Transaction - Procces Claim Request</h4>

<div class="card mb-4">
    <div class="card-body">

        <div class="mb-3 row">
            <label asp-for ="TransType" class="col-md-2 col-form-label">Transaction Type</label>
            <div class="col-md-4">
                @Html.DropDownListFor(model => model.TransType, (IEnumerable<SelectListItem>)ViewData["TransactionTypes"], "-- Select All --", new { @class = "form-control" })
                <span asp-validation-for="TransType" class="text-danger"></span>
            </div>
            <label asp-for="CompanyName" class="col-md-2 col-form-label">Company Name </label>
            <div class="col-md-4">
                @Html.DropDownListFor(model => model.CompanyName, (IEnumerable<SelectListItem>)ViewData["DDLCompanyProcessClaimRequest"], "-- Select All --", new { @class = "form-control" })
                <span asp-validation-for="CompanyName" class="text-danger"></span>
            </div>

        </div>

        <div class="mb-3 row">
          
       
        </div>

    </div>
</div>
@using (Html.BeginForm("Index", "Pension", FormMethod.Get))
{

    <div class="card-mb-4">
        <div class="card-header">
            <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">  </span> Table</h4>

            <div class="table-responsive text-nowrap">
                <table class="table">
                    <div class="row">
          
                    </div>
            </div>
        </div>
    </div>
}
<thead>
    <tr>
        <th>
            @*            @Html.DisplayNameFor(model => model.FirstOrDefault().DepartmentId)
            *@
        </th>
        <th>
            @*            @Html.DisplayNameFor(model => model.FirstOrDefault().Name)
            *@
        </th>
        <th>
            @*            @Html.DisplayNameFor(model => model.FirstOrDefault().GroupName)
            *@
        </th>
        <th>
            <a asp-action="Index" asp-route-sortOrder="@ViewBag.DepartmentId">
            </a>
        </th>
        <th></th>
    </tr>
</thead>
<tbody>
    @*    @foreach (var item in Model)
    *@
    <tr>
        <td>
            @*                @Html.DisplayFor(modelItem => item)
            *@
        </td>
        <td>
            @*                @Html.DisplayFor(modelItem => item)
            *@
        </td>
        <td>
            @*                @Html.DisplayFor(modelItem => item)
            *@
        </td>
        <td>
            <div class="dropdown">
                <button type="button"
                        class="btn p-0 dropdown-toggle hide-arrow"
                        data-bs-toggle="dropdown">
                    <i class="bx bx-dots-vertical-rounded"></i>
                </button>
                <div class="dropdown-menu">
                    <a class="dropdown-item" asp-action="Edit" asp-route-id=""><i class="bx bx-edit-alt me-1"></i>Edit</a>
                    <a class="dropdown-item" asp-action="Details" asp-route-id=""><i class="bx bx-detail me-1"></i>Details</a>
                    <a class="dropdown-item" asp-action="Delete" asp-route-id=""><i class="bx bx-trash me-1"></i>Delete</a>
                </div>
            </div>
        </td>
    </tr>


</tbody> </table>

<div class="mb-5">
    <h6 class="mt-2 mb-3 text-muted">Action</h6>
    <nav class="navbar navbar-example navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <button class="navbar-toggler"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#navbar-ex-2"
                    aria-controls="navbar-ex-2"
                    aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbar-ex-2">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <button type="button" class="btn btn-outline-danger mx-2">Save</button>
                    </li>

                </ul>
            </div>
        </div>
    </nav>
</div>


