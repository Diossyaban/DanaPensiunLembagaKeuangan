@model X.PagedList.IPagedList<DPLK.Models.SecType>
@using X.PagedList.Mvc.Core

<link href="~/css/PagedList.css" rel="stylesheet" type="text/css" />
@{
    ViewData["Title"] = "DPLK | Perta Life";
    Layout = "~/Views/Shared/DPLK_Layout.cshtml";
}

<div class="container-xxl flex-grow-1 container-p-y">
    <div class="table-responsive text-nowrap">
        <div class="card-header">
            <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Pension /</span> Parameter Maker - Sec Type</h4>
            <a class="btn btn-primary text-end" asp-action="ParameterMakerSecTypeCreate"><span class="tf-icons bx bx-book-add"></span>&nbsp; Create New</a>

            <div class="row mt-3">
                <div class="col-md-6">
                    <form asp-action="ParameterMakerSecTypeIndex" method="get">
                        <div class="input-group mb-3">
                            <input type="text" name="searchString" class="form-control" placeholder="Search...">
                            <button class="btn btn-outline-secondary" type="submit">Search</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>
                            <a asp-action="ParameterMakerSecTypeIndex" asp-route-sortOrder="@ViewBag.SecTypeNmbr" asp-route-page="1">ID</a>
                        </th>
                        <th>
                            <a asp-action="ParameterMakerSecTypeIndex" asp-route-sortOrder="@ViewBag.SecTypeNm" asp-route-page="1">Description</a>
                        </th>
                        <th>
                            <a asp-action="ParameterMakerSecTypeIndex" asp-route-sortOrder="@ViewBag.OldTypeNmbr" asp-route-page="1">Old ID</a>
                        </th>
                        <th>
                            <a asp-action="ParameterMakerSecTypeIndex" asp-route-sortOrder="@ViewBag.LastChangeDt" asp-route-page="1">Last Change Date</a>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var secType in Model)
                    {
                        <tr>
                            <td>@secType.SecTypeNmbr</td>
                            <td>@secType.SecTypeNm</td>
                            <td>@secType.OldTypeNmbr</td>
                            <td>@secType.LastChangeDt</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
