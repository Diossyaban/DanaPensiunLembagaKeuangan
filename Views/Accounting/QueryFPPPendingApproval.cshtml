@model X.PagedList.IPagedList<DPLK.ModelAcc.SpdJurnalTemp>
@using X.PagedList.Mvc.Core
@using X.PagedList.Web.Common;

<link href="~/css/PagedList.css" rel="stylesheet" type="text/css" />
@{
    ViewData["Title"] = "DPLK | Perta Life";
    Layout = "~/Views/Shared/DPLK_Layout.cshtml";
}
<h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Accounting /</span> Query FPP Pending Approval</h4>

<div class="container-fluid px-4">
    <div class="card my-4">

        <div class="card-header">
            <div class="col-md-6">
                <form asp-action="QueryFPPPendingApproval" method="get">
                    <div class="input-group mb-3">
                        <input type="text" name="searchString" class="form-control" placeholder="Search...">
                        <button class="btn btn-outline-secondary" type="submit">Search</button>
                    </div>
                </form>
            </div>
        </div>
        <div class="card-body">
            <div class="table-responsive text-nowrap">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th></th>
                            <th>ID</th>
                            <th>Description</th>
                            <th>Jurnal Date</th>
                            <th>Amount</th>
                            <th>Fund</th>
                            <th>Correction</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>

                                    <div class="dropdown">
                                        <button type="button"
                                                class="btn p-0 dropdown-toggle hide-arrow"
                                                data-bs-toggle="dropdown">
                                            <i class="bx bx-dots-vertical-rounded"></i>
                                        </button>
                                        <div class="dropdown-menu">
                                            <a class="dropdown-item" asp-action="" asp-route-id="@item."><i class="bx bx-edit-alt me-1"></i>Edit</a>
                                            <a class="dropdown-item" asp-action="" asp-route-id="@item."><i class="bx bx-trash me-1"></i>Delete</a>
                                        </div>
                                    </div>

                                </td>
                                <td>@item.TrnsId</td>
                                <td>@item.Description</td>
                                <td>@item.DateJurnal.ToShortDateString()</td>
                                <td>@item.Debet</td>
                                <td>@item.Description</td>

                                <td>
                                    @if (item.Correction.HasValue)
                                    {
                                        if (item.Correction == false)
                                        {
                                            <span class="badge bg-label-secondary me-1 hover">Not Specified</span>
                                        }
                                        else if (item.Correction == true)
                                        {
                                            <span class="badge bg-label-success me-1 hover"> Correction</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-label-warning me-1 hover">Undefined</span>
                                        }
                                    }
                                    else
                                    {
                                        <span class="badge bg-label-secondary me-1 hover">Not Specified</span>
                                    }
                                </td>


                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="text-center mt-4">
    Page @(Model.PageNumber) of @Model.PageCount
    @Html.PagedListPager(Model, page => Url.Action("QueryFPPPendingApproval", new { page, currentFilter = ViewBag.CurrentFilter }), PagedListRenderOptions.TwitterBootstrapPager)
</div>
